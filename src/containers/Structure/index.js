import React, { Component } from 'react';
import PageContainer from '../../components/PageContainer';

const data = `
`

export default class Structure extends Component {
  render() {
    return <PageContainer title="構成" data={data} />
  }
}
